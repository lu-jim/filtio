<div class="container mx-auto px-4 py-8">
  <div class="max-w-4xl mx-auto">
    <div class="flex justify-between items-start mb-8">
      <div>
        <h1 class="text-3xl font-bold text-gray-900 mb-2">Edit <%= @company.name %></h1>
        <p class="text-gray-600">Update company information and manage founders.</p>
      </div>
      <div class="flex space-x-3">
        <%= link_to 'View Company', @company, class: 'bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg transition duration-200' %>
      </div>
    </div>

    <%= render 'form', company: @company %>
  </div>
</div>
